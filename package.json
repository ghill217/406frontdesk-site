{
  "name": "406-frontdesk-site",
  "version": "1.0.0",
  "description": "406 Front Desk marketing site — static, built with Eleventy, deployed on Netlify.",
  "private": true,
  "scripts": {
    "build": "eleventy",
    "start": "eleventy --serve --quiet",
    "dev": "eleventy --serve",
    "audit": "node scripts/a11y-audit.mjs",
    "check:build-request": "node scripts/build-request-checks.mjs",
    "check:nav": "node \"C:/Users/ghill/Documents/Obsidian/The Hive Mind/.claude/scripts/check-nav-occlusion.mjs\" dist",
    "check:discovery": "node netlify/functions/discovery-verdict.mjs --selftest && node scripts/dev-discovery.mjs --selftest && node scripts/check-discovery.mjs",
    "sync:discovery": "node scripts/sync-discovery.mjs"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.0.0"
  },
  "dependencies": {
    "@netlify/blobs": "^11.0.2"
  }
}
